# 볼드 인접 규칙 테스트

읽기 모드(Ctrl+E)로 보세요. 확인 끝나면 이 파일은 지우셔도 됩니다.

| # | 케이스 | 결과 |
| --- | --- | --- |
| 1 | 닫는 ** 앞이 `)`, 뒤에 한글 붙임 | 아래 확인 |
| 2 | 닫는 ** 앞이 한글, 뒤에 한글 붙임 | 아래 확인 |
| 3 | 닫는 ** 앞이 `)`, 뒤에 공백 | 아래 확인 |
| 4 | 괄호 안만 볼드 | 아래 확인 |
| 5 | 조사까지 볼드에 포함 | 아래 확인 |
| 6 | HTML 태그 | 아래 확인 |
| 7 | 닫는 ** 앞이 `)`, 뒤가 문장부호 | 아래 확인 |

---

**1.** 멤버 함수에는 **한정자(const)**가 존재한다.

**2.** 데이터 멤버에는 **일반적으로**를 사용한다.

**3.** 멤버 함수에는 **한정자(const)** 가 존재한다.

**4.** 멤버 함수에는 한정자(**const**)가 존재한다.

**5.** 멤버 함수에는 **한정자(const)가** 존재한다.

**6.** 멤버 함수에는 <b>한정자(const)</b>가 존재한다.

**7.** 멤버 함수에는 **한정자(const)**, 그리고 또 있다.

---

각 줄에서 볼드가 **어디서 끝나는지**와 `**`가 글자로 보이는지를 확인하세요.
